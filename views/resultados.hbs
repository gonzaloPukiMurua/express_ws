<div class="container">
    <div class="row">
        <div class="col-md-4">
            <div class="card">
                <div class="card-body">
                    <h2 class="card-title" id="bolilla">1</h2>
                    <h3 class="card-subtitle" id="orden">1</h3>
                </div>
            </div>
        </div>
    </div>
</div>
<script src="https://cdn.socket.io/4.5.4/socket.io.min.js"></script>
<script>
  const socket = io();
  const bolilla = document.getElementById('bolilla');
  const orden = document.getElementById('orden');   
  socket.on("connect", () => {
  socket.emit('id', socket.id); // "G5p5..."
  });
  socket.on('next', (msg) => {
    console.log('Recibido: ', msg);
    bolilla.innerHTML = `${msg}`;
    orden.innerHTML = `${msg}`;
  });
  
</script>